# Organization and project keys are displayed in the right sidebar of the project homepage
sonar.organization=slash-foundry
sonar.projectKey=mapogram-webapp
sonar.host.url=https://sonarcloud.io

# --- optional properties ---

# defaults to project key
#sonar.projectName=My project
# defaults to 'not provided'
#sonar.projectVersion=1.0

# sonar.coverage.exclusions=./src/__tests__/**/*,**/**/*.test.ts,*.config.js,*.setup.js,**/utils/**,**/serverless.ts,**/express.ts,**/migrations/**
# sonar.exclusions=./src/__tests__/**/*,**/**/*.test.ts,*.config.js,*.setup.js,/src/**/models/**.ts,**/utils/**,**/serverless.ts,**/express.ts,**/migrations/**
# sonar.test.inclusions=./src/__tests__/**

# Path is relative to the sonar-project.properties file. Defaults to .
sonar.sources=.

sonar.javascript.node.maxspace=4096
# sonar.javascript.lcov.reportPaths=./coverage/lcov.info
# sonar.testExecutionReportPaths=test-report.xml
sonar.eslint.reportPaths=eslint-report.json

# Encoding of the source code. Default is default system encoding
sonar.sourceEncoding=UTF-8
